#include <bits/stdc++.h>
using namespace std;

int main() {
    int N;
    cin>>N;
    int A[N],B[N],C[N],D[N];
    for (int i = 0; i <= N; i++){
        cin >> A[i] >> B[i] >> C[i] >> D[i];
    }
    
}